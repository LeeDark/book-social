# Task Template for Codex

Task:

Context:
- Read `AGENTS.md`.
- Read `docs/ai/project-context.md`.
- Check `docs/ai/task-history.md` for recent decisions.

Goal:

Scope:

Out of scope:

Constraints:
- Keep changes minimal.
- Do not rewrite architecture.
- Do not introduce heavy dependencies.
- `go test ./...` must pass.

Expected result:

Summary required:
- What changed
- What tests were added/updated
- What commands were run
- What was intentionally left for later