apiVersion: v1
kind: Service
metadata:
  name: book-social-api
spec:
  selector:
    app: book-social-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
  